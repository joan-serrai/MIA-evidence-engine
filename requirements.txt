# MIA dependencies (direct ones only).
# Install with:  ./.venv/Scripts/python.exe -m pip install -r requirements.txt
#
# Versions are PINNED with "==" on purpose: they are the ones verified together on
# Windows 11 + Python 3.12. To upgrade one, bump it, test the pipeline, and pin the
# new version here. Never by accident.
#
# `requirements.lock.txt` is the full `pip freeze` of the verified environment; use it
# to reproduce the exact setup (the guided installer does).

# --- Data: download evidence from the public APIs ---
requests==2.34.2              # PubMed and ClinicalTrials.gov calls
python-dotenv==1.2.3          # read settings from the .env file

# --- Vector database ---
chromadb==1.5.9               # local vector store (one collection per profile)

# --- Embeddings ---
sentence-transformers==6.0.1  # alternative backend (BAAI/bge-small-en-v1.5)
transformers==5.16.1          # MedCPT: src/embeddings.py uses AutoTokenizer/AutoModel
torch==2.13.0                 # compute engine for the two above
numpy==2.5.2                  # vectors

# --- Local LLM ---
ollama==0.6.2                 # Python client of the local Ollama server

# --- Interface and tabular data ---
streamlit==1.62.0             # the app
pandas==3.0.5                 # tables
tqdm==4.70.0                  # progress bars while downloading / processing

# --- Development only (unit tests; not needed to run MIA) ---
# `tests/test_pure.py` covers the pure functions (citations, figures, guards, domain
# profiles) without Ollama or ChromaDB. Run:  python -m pytest -q
pytest==9.0.2
